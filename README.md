# Vite + React + Firebase Starter

## Setup
1. Clone: `git clone repo_url`
2. Install: `npm i`
3. Tạo file `.env` từ `.env.example` và điền key Firebase
4. Run: `npm run dev`

## Đã có sẵn
- Tailwind CSS
- Firebase Realtime Database
- Components: Button, Input
- Hooks: useApi, useLocalStorage